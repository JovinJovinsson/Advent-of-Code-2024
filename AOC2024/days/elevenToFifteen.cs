public class ElevenToFifteen
{
    #region Day Eleven
    /// <summary>
    /// Solves the Day 11 portion of the AOC 2024 challenge.
    /// https://adventofcode.com/2024/day/11
    /// </summary>
    public void DayEleven()
    {
        // The input files
        string fileName = "assets/elevenToFiften/AOC2024.11.Test-Input copy.txt";
        // string fileName = "assets/elevenToFiften/AOC2024.11.Input.txt";

        // Read the data in from the text file
        using (StreamReader streamReader = new StreamReader(fileName))
        {
            // Placeholder for the current line of the tile
            string currentLine;

            // currentLine will be null when the StreamReader reaches the end of file
            while((currentLine = streamReader.ReadLine()) != null)
            {
            }
        }
    }
    #endregion
}